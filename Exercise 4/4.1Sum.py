# Input two integers and print out their sum. Preserve the exact format from the examples 
# (e.g. the output should contain exactly three lines)

k=int(input("a= "))
g=int(input("b= "))
d=k+g
print("k+g=",f"{k}+{g}","=", d)